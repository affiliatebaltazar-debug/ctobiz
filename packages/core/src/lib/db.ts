// Database connection — placeholder for Drizzle setup
// In production, this will connect to SQLite via better-sqlite3

export function getDb() {
  // Placeholder — actual implementation will use Drizzle + better-sqlite3
  return null;
}
